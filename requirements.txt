# Inference dependency versions observed in the authors' evaluated environment.
# Install a matching CUDA-enabled torch/torchvision pair first (see docs/INSTALL.md).
# SAM 2 and Depth Anything repositories/models are installed separately.
# This is a direct-dependency list, not a claim of a clean-room full lockfile.
accelerate==1.0.1
diffusers==0.31.0
peft==0.13.2
transformers==4.46.1
tokenizers==0.20.1
huggingface-hub==0.26.2
safetensors==0.4.5
numpy==1.26.4
scipy==1.14.1
Pillow==9.5.0
opencv-python==4.10.0.84
einops==0.8.0
gradio==5.3.0
gradio_client==1.4.2
hydra-core==1.3.2
omegaconf==2.3.0
iopath==0.1.10
PyYAML==6.0.2
matplotlib==3.9.2
tqdm==4.66.6
packaging==24.1
